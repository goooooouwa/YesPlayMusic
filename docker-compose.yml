services:
  yesplaymusic:
    build:
      context: .
    image: yesplaymusic
    container_name: yesplaymusic
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
    #expose:
    #  - 80
    restart: always

networks:
  default:
    name: nginx4omv_default
    external: true
